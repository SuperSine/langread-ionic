generates:
  ./src/app/graphql-components.ts:
    schema: 
      - "./schema.graphql"
      - "./auth-schema.graphql"
    documents: src/app/services/graphql/*.graphql
    plugins:
      - typescript
      - typescript-operations
      - typescript-apollo-angular