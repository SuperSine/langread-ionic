import { AvatarUrlDirective } from './avatar-url.directive';

describe('AvatarUrlDirective', () => {
  it('should create an instance', () => {
    const directive = new AvatarUrlDirective();
    expect(directive).toBeTruthy();
  });
});
