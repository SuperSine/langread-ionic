import { PickDirective } from './pick.directive';

describe('PickDirective', () => {
  it('should create an instance', () => {
    const directive = new PickDirective();
    expect(directive).toBeTruthy();
  });
});
