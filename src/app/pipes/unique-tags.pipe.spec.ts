import { UniqueTagsPipe } from './unique-tags.pipe';

describe('UniqueTagsPipe', () => {
  it('create an instance', () => {
    const pipe = new UniqueTagsPipe();
    expect(pipe).toBeTruthy();
  });
});
